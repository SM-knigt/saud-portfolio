# https://docs.netlify.com/configure-builds/file-based-configuration/
# Site subdomain (Netlify UI): saudb-portfolio → https://saudb-portfolio.netlify.app
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
