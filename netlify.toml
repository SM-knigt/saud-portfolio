# https://docs.netlify.com/configure-builds/file-based-configuration/
# Site subdomain (set in Netlify UI): saud-portfolio → https://saud-portfolio.netlify.app
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
